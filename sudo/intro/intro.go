package intro
import "fmt"


func Name(name string) string {
	message := fmt.Sprintf("Hello there! My name is %v", name)
	return message
}

func Address(address string) string {
	message := address
	return message
}